# O popup "Participar da Comunidade" agora salva os inscritos no back-end
# próprio (POST /subscribe), que envia o e-mail de boas-vindas pelo Resend.
# O ConvertKit não é mais usado.

# URL base do back-end do Sasso Nomad (Fase 2/3). Em desenvolvimento local
# aponta pro servidor Express rodando em localhost:3000.
VITE_API_URL=http://localhost:3000
